#ifndef LARK_PROJECT_BATTLE_H
#define LARK_PROJECT_BATTLE_H

#include <string>
#include <vector>
#include "Player.h"
#include "Enemy.h"

void battle(const Player &, const Enemy &, const Enemy &, const Enemy &, const Enemy &, const std::vector<std::string> &, const std::vector<std::string> &);

#endif //LARK_PROJECT_BATTLE_H
